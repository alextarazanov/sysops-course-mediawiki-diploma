[Unit]
Description=PostgreSQL Logical Backup (pg_dumpall)
After=postgresql.service
Requires=postgresql.service

[Service]
Type=oneshot
User=postgres
ExecStart=/home/ansible/postgres_backup.sh
StandardOutput=journal
StandardError=journal
TimeoutSec=3600